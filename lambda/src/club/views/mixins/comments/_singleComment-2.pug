include _singleComment-1

mixin writeComment2(comment)
    ul
        -//.avatar 
        -//    p #{comment.userId}
        .comment-content
            header
                a #{comment.userId}
        p #{comment.text}
        each child in comment.comments
            +writeComment(child)
