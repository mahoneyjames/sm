extends sc-layout
include mixins/_themeInfo
block content
    
    //-pre= helpers.dump(themes)
    //- section.themes
    //-     each year in themesGrouped    
    //-             h1.year #{year.year}
    //-             each theme in year.themes
    //-                 .theme
    //-                     h1 
    //-                         a(href=`${theme.path}`) #{theme.themeText}
    //-                     ul
    //-                         each thing in theme.things
    //-                             li #{thing}

    
    ul.innerNav
        each navYear in years
            li(class=(year===navYear?'selected':''))
                a(href=`themes-${navYear}`) #{navYear}
    section.themes
        each theme in themes
            .theme
                h1 
                    a(href=`${theme.path}`) #{theme.themeText}
                ul
                    each thing in theme.things
                        li #{thing}

                        